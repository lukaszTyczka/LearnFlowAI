---
import BaseLayout from '../layouts/BaseLayout.astro';
// Import the wrapper component
import { RegisterWrapper } from '../components/auth/RegisterWrapper'; 

// Middleware should redirect logged-in users away from this page
---
<BaseLayout title="Register - LearnFlowAI">
  <div class="container mx-auto mt-10">
     {/* Render the RegisterWrapper as the island */}
    <RegisterWrapper client:load />
  </div>
</BaseLayout> 